import 'package:flutter/material.dart';

import 'components/body.dart';

class RentHomeScreen extends StatelessWidget {
  static String routeName = "/renthome";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
